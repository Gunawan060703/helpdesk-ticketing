[phases.setup]
nixPkgs = ["php81Extensions.pdo_mysql", "php81Extensions.mysqli", "php81"]

[phases.install]
cmds = ["composer install --no-dev"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"